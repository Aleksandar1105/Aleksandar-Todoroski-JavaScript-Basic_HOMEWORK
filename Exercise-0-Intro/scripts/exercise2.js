/* Write a JavaScript program that will read the two sides of a rectangle 
and calculate its area
TIP: area = a * b */

let sideA = parseFloat(prompt('Enter the value for side a:'));
let sideB = parseFloat(prompt('Enter the value for side b:'));

let area = sideA * sideB;
console.log('The area of rectungle is:', area);